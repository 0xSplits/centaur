# Hoist tslib to the root node_modules. discord.js (used by services/discordbot) ships its
# source and requires tslib; with pnpm strict layout, Bun cannot resolve the nested .pnpm symlink
# ("Cannot find package 'tslib'"). Public-hoisting makes it resolvable by walking up to root.
public-hoist-pattern[]=tslib
